// Stub — dk-repl completions are daakia-specific and not used in standalone DUI.
export const DK_TYPE_DEFS = '';
export function getDkCompletions(_text: string, _monaco: unknown, _pos: unknown): unknown[] {
  return [];
}
